/* @flow */

import React from 'react';

const displayName = 'pages.Dashboard.Loading';

const Loading = () => <div style={{ height: '100vh' }} />;

Loading.displayName = displayName;

export default Loading;
