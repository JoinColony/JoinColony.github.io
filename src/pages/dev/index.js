/* @flow */

import { createElement } from 'react';

import Developers from '~pages/Developers';

const DevelopersIndex = (props: Object) => createElement(Developers, props);

export default DevelopersIndex;
