/* @flow */

import { createElement } from 'react';

import ProductApp from '~pages/Website/ProductApp';

const ProductAppPage = (props: Object) => createElement(ProductApp, props);

export default ProductAppPage;
