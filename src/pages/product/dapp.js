/* @flow */

import { createElement } from 'react';

import ProductDapp from '~pages/Website/ProductDapp';

const ProductDappPage = (props: Object) => createElement(ProductDapp, props);

export default ProductDappPage;
