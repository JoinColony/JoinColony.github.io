/* @flow */

import { createElement } from 'react';

import HomePage from '~pages/Website/HomePage';

const WebsiteHomePage = (props: Object) => createElement(HomePage, props);

export default WebsiteHomePage;
